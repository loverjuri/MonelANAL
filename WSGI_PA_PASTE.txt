# Скопируйте ВСЁ НИЖЕ (от import sys до application) в PA → Web → WSGI configuration file
# ---

import sys
path = '/home/monocore/MonelANAL'
if path not in sys.path:
    sys.path.insert(0, path)

# Если pyotp/flask не находятся — проверьте: ls venv/lib/ — там python3.10 или python3.13?
venv_site = '/home/monocore/MonelANAL/venv/lib/python3.13/site-packages'
if venv_site not in sys.path:
    sys.path.insert(0, venv_site)

from app import application
