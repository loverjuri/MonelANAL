# Скопируйте ВСЁ НИЖЕ в PA → Web → WSGI configuration file.
# Переменные окружения задайте в Environment variables PythonAnywhere.
# ---

import sys
from pathlib import Path

project_dir = Path('/home/monocore/MonelANAL')
if str(project_dir) not in sys.path:
    sys.path.insert(0, str(project_dir))

from app import app as application
